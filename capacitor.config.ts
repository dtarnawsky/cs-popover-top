import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.popover.top',
  appName: 'cs-popover-top',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
